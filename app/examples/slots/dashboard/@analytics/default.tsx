// @analytics/default.tsx — fallback rendered when this slot has no active match.
// Without this file Next.js throws on direct navigation to any URL that doesn't
// explicitly render this slot (e.g. navigating to /dashboard/team-detail).

export { default } from "./page";
